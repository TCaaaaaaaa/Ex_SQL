graph LR
    User[用户 (Browser)] -- TypeScript/React --> Frontend[前端页面]
    Frontend -- REST API (JSON) --> Backend[后端 API (FastAPI)]
    
    subgraph "Python 后端核心"
        Backend -- 调用 --> Parser[SQL 解析模块 (sqlglot)]
        Backend -- 调用 --> Engine[可拓计算引擎 (Extenics Lib)]
        Engine -- 读取 --> KB[知识/规则库]
    end
    
    subgraph "数据持久化"
        Backend -- 读写 --> DB[(PostgreSQL)]
        Backend -- 缓存 K值 --> Redis[(Redis)]
    end